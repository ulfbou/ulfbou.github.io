@using System.Reflection.Metadata
<MudContainer>
    <MudText Typo="Typo.h4">@ContentTitle</MudText>
    @if (!string.IsNullOrEmpty(ContentHtml))
    {
        <MudText Typo="Typo.body1">@((MarkupString)ContentHtml)</MudText>
    }
</MudContainer>
